Marketing page images. docs/10-design-system.md.

Served from /img/, which middleware.ts passes through so the apex does not
rewrite it into the /site tree. Real situations or loose ink -- never stock.
